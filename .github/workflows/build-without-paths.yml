name: build

on:
  push:
    paths-ignore:
      - 'src/**'
      - 'tests/**'
      - '.github/**'
    branches:
      - '**'
  pull_request:
    paths-ignore:
      - 'src/**'
      - 'tests/**'
      - '.github/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "No build required"'
